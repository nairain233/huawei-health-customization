# AGENTS.md

## 项目概述

本项目实现一个基于现代 libxposed API 102 的 LSPosed 模块，用于精简华为运动健康的界面布局，并支持用户明确选择的服务调用屏蔽。
静态定位依据见 [布局精简分析](docs/布局精简分析.md)，开发约束见 [精简 UI Hook 指南](docs/精简UI-Hook指南.md)。
## 项目边界

- 处理界面布局精简，以及用户逐项选择的华为运动健康显式服务启动与绑定屏蔽；服务功能默认关闭，独立于布局开关。
- 服务屏蔽仅限目标主进程主动发起的调用，不立即停止已运行服务，不禁用系统组件，不扩展至广播或定时任务。不得将服务标注为已确认可安全关闭，除非有实际证据。
- 默认不修改健康数据、运动记录、设备连接、账号登录、数据同步和权限流程。
- 默认不拦截网络请求，不伪造会员、健康或设备状态。
- 模块作用域只包含华为运动健康，不得扩大到系统框架或无关应用。
- 尚未确认的目标应用版本、页面和 Hook 点必须标记为待确认，不得凭空填写。

## 模块主要构成

### 1. Android 模块应用

模块 APK 同时作为 LSPosed 模块载体和可选的配置应用。Android 工程负责资源、设置界面、模块元数据、构建与发布。

### 2. API 102 模块元数据

`app/src/main/resources/META-INF/xposed/` 计划包含：

- `module.prop`：声明 `minApiVersion=102`、`targetApiVersion=102` 和作用域模式。
- `java_init.list`：声明继承 `XposedModule` 的模块入口类。
- `scope.list`：声明默认作用域，最终仅保留目标应用包名。

元数据必须与实际入口类、支持范围和依赖版本保持一致。

### 3. 模块入口

入口类继承 `io.github.libxposed.api.XposedModule`，负责接收 API 102 生命周期回调。入口只做以下工作：

- 校验目标包名与进程。
- 初始化脱敏日志。
- 分发 `onPackageLoaded`、`onPackageReady` 等生命周期事件。
- 调用功能注册表安装 Hook。

不得在入口类中堆积具体页面的布局处理逻辑。

### 4. 包与进程守卫

所有 Hook 安装前必须经过统一守卫：

- 仅匹配目标应用包名。
- 默认只处理主进程；子进程必须有明确用途后再单独加入。
- 同一进程内的 Hook 安装必须幂等，避免重复注册。
- 条件不满足时直接跳过，不影响目标应用启动。

### 5. 功能注册与页面 Hook

每个精简项作为独立功能单元，至少包含：

- 唯一、稳定的功能标识。
- 默认启用状态。
- 目标页面或组件说明。
- Hook 安装入口。
- 安装失败时的安全降级行为。

不同页面的 Hook 不得互相依赖。单个功能失败时，仅禁用该功能并记录原因，不得中断其他功能或让目标应用崩溃。

### 6. Hook 目标解析

Hook 目标解析与界面修改逻辑分离。解析器负责定位类、方法、字段或资源，页面 Hook 只使用解析结果。

目标选择优先级：

1. 稳定的页面生命周期、Fragment、Adapter 或数据绑定入口。
2. 稳定的方法签名、字段类型和调用关系。
3. 资源名称或资源 ID。
4. 显示文本、层级位置等易变化特征，仅作为受限后备方案。

不得猜测混淆类名或资源 ID。新增定位规则时，应记录定位依据和失效后的行为。

### 7. 布局操作工具

通用布局操作集中封装，避免每个功能重复实现：

- 设置 `View.GONE` 或恢复原始可见性。
- 安全移除视图。
- 调整间距、尺寸与布局参数。
- 在主线程执行界面修改。
- 防止重复处理同一视图。

处理 `RecyclerView`、列表 Adapter 或可复用视图时，必须同时定义恢复逻辑，避免隐藏状态污染其他列表项。

### 8. 配置系统

若提供模块设置界面，配置计划由以下部分组成：

- 模块应用通过 `io.github.libxposed:service:102.0.0` 连接 `XposedService`。
- 使用 `RemotePreferences` 保存每个精简项的开关。
- 目标应用进程只读取模块配置，不直接访问模块私有文件。
- 配置键集中定义，并提供明确默认值。
- 配置读取失败时采用安全默认值，不得阻塞目标应用。

是否支持实时生效应由具体功能决定；不得把 API 102 Hot Reload 当作普通配置同步机制。

### 9. 日志与错误隔离

- 使用 libxposed 提供的日志能力记录模块生命周期、Hook 安装结果和失败原因。
- 日志不得包含账号、设备标识、运动记录、健康数据或其他个人信息。
- 反射、类加载和 Hook 安装必须局部捕获异常。
- 不允许空的异常处理；跳过功能时应留下可诊断但已脱敏的信息。
- 发布构建默认避免输出高频调试日志。

### 10. 配置界面

配置界面仅负责展示模块状态和管理精简项开关，主要区域计划包括：

- 模块与服务连接状态。
- 总开关。
- 按页面分组的功能开关。
- 当前版本与诊断信息。

界面不得直接承载 Hook 逻辑。未实现设置界面前，应保持模块行为简单并提供安全默认值。

### 11. 构建与依赖

- libxposed API 必须使用 `compileOnly("io.github.libxposed:api:102.0.0")`，避免打包框架 API。
- 仅在需要模块配置界面时引入 `io.github.libxposed:service:102.0.0`。
- 新依赖必须解决当前明确需求，不为假设中的未来功能提前引入。
- 优先沿用当前 Android Gradle Plugin、JVM 21 和版本目录配置。
- 若决定使用 Kotlin，必须先完整配置 Kotlin Android 插件，不得依赖当前示例测试文件推断 Kotlin 已可用。

## 建议目录结构

```text
app/src/main/
├── java/love/nairain/huawei/
│   ├── app/                 # 模块应用与配置界面
│   ├── config/              # 配置键、读取与默认值
│   ├── hook/
│   │   ├── HookEntry        # API 102 模块入口
│   │   ├── HookRegistry     # 功能注册与分发
│   │   ├── guard/           # 包名、进程和安装守卫
│   │   ├── feature/         # 各页面独立精简功能
│   │   ├── resolver/        # Hook 目标解析
│   │   └── util/            # View、反射和日志工具
│   └── model/               # 配置或诊断模型
├── resources/META-INF/xposed/
│   ├── java_init.list
│   ├── module.prop
│   └── scope.list
└── res/                     # Android 资源
```

目录可随实际需求缩减。仅有一个实现时不要为了匹配目录图创建空抽象或空包。

## 开发规则

- 先读取目标应用的实际代码、资源或运行时证据，再编写 Hook。
- 一个功能单元只负责一个明确的精简行为。
- 优先使用清晰直接的实现，避免过早引入复杂扫描框架。
- 两处以上出现真正相同的布局操作时，再提取公共工具。
- 所有 Hook 必须可独立关闭，并具备失败跳过路径。
- 禁止在主线程执行耗时的 Dex 扫描或磁盘操作。
- 禁止持有 Activity、Fragment 或 View 的静态强引用。
- 代码注释语言跟随现有文件；新增项目文档与业务注释默认使用简体中文。
- 每次 Plan 完成后，必须在最终回复中总结该次任务的实际变更，并用独立代码块给出一条可直接使用的中文 commit 信息；采用 `type: 中文描述` 格式，内容准确、简洁，覆盖本次变更。
- 未经用户明确要求，不创建分支，不执行 `git commit` 或 `git push`。

## 验证要求

新增或修改 Hook 后至少验证：

1. 模块能够编译，APK 内包含正确的 API 102 元数据。
2. 非目标包和非目标进程不会安装 Hook。
3. 关闭功能后目标界面保持原始行为。
4. 单个 Hook 解析失败时目标应用不会崩溃。
5. 页面重复进入、返回和列表滚动后布局状态一致。
6. 日志不包含账号、设备、运动或健康隐私数据。
7. 登录、首页、运动记录、设备连接和同步等未涉及流程不受影响。

## 实现概要

- schema v2 统一使用 `hide.health.*`、`hide.sport.*`、`hide.device.*`、`hide.mine.*`、`hide.bottom.*`；保留 `enabled` 与 `hide_launcher_icon` 原值，旧 `row_*` / `tab_*` 不迁移也不读取。
- `onPackageReady` 只安装 `Application.attach()` Hook；attach 完成后读取真实包信息和一次性配置快照。服务独立安装，布局经后台 DexKit 扫描或缓存复核后按能力安装。
- 健康页按 `HomeCardAdapter` 副本和 `FunctionMenuCardData`/`HealthQuickEntryCardData` 顶层卡片标识过滤；快捷入口关闭时整张卡片移除，不解析其中的单个入口。运动页 Knit 过滤仅限 `SportTabPageResTrigger` 的 `resPosId=4040`；设备页按已核验资源名折叠；我的页按资源名及 `wrq/wrl/wrb/wsa` 模型过滤并清理空分组；营销卡片仅在 `PersonalCenterRecyclerViewAdapter$c$4` 的 `d(Map)` 回调中移除已核验的 4168/9013。
- 底栏保留原始 Tab 索引，使用按 `HealthBottomView` 实例隔离的弱状态表，处理清空、重复布局和 RTL。
- DexKit 扫描 base/split APK，结果按功能隔离并缓存；当前规则版本 3 共检查 65 个布局开关，保留 `mine.marketing` 独立能力组并移除健康卡片精简能力。未知版本只启用唯一命中且签名、内容身份通过校验的功能；历史数字 ID/文案后备仅限已核验版本。不持有 Activity、Fragment 或 View 的静态强引用。
- 首页展示扫描 m/n 与预约重扫；`scan.request` 由模块写入，目标下次启动后执行。报告通过验证 UID 的专用 Provider 回传，不修改用户开关。规则变化递增 `ScanProtocol.RULES`，详细依据见 [DexKit 适配说明](docs/DexKit适配说明.md)。
- 服务功能使用 `service_block.enabled` 和 `service_block.components`，保留 schema v2 布局配置；attach 完成后独立安装，不依赖布局扫描或宿主版本白名单。通过已核验的 ContextImpl 内部入口匹配显式组件，缺少声明的旧规则不生效，安装不完整时回滚并放行。
- 服务绑定状态按外层 Context 与 ServiceConnection 对象身份弱引用隔离，优先保留真实解绑；不记录 Intent 内容和宿主异常消息。设备验证边界见 [后台服务精简说明](docs/后台服务精简说明.md)。
- 配置应用的首页与五类布局页统一订阅 `LayoutSettingsCoordinator`：应用级单线程串行执行 `getRemotePreferences()`、读取、默认值补齐和写入，主线程只接收已确认状态。`LayoutConfigStore` 使用 `commit()` 确认写入，失败后按原值及原始存在状态执行第二次 `commit()` 回滚；回滚失败时当前服务绑定被标记为不确定，必须等待新的服务绑定后才能恢复编辑。页面保存期间不乐观切换，默认补齐失败但已恢复时允许继续编辑并显示文字提示。
- 配置应用提供简体中文与 English 完整资源，默认跟随系统；“主题设置”中的语言下拉通过 AppCompat 应用语言 API 在 Android 9–最新版本持久化并立即刷新。语言只影响模块配置应用，不写入 RemotePreferences，也不改变宿主中文内容定位规则。
- 第一阶段配置可靠性验证包含 63 项 JVM 测试（0 失败、1 项因缺少桌面 DexKit native 环境跳过）及 31 项 instrumentation 测试源码；AndroidTest APK 已构建。当前无设备或 AVD，设置页加载、保存、失败、重连、快速切换与持久化重启的运行时 UI 验证保留到第九阶段。
- 第三阶段（2026-09-15）：`ScanStatusController` 用运行会话与绑定身份隔离任务，主线程独占 UI 状态；停止先使旧会话失效，再关闭执行器。两秒轮询保留，换绑主动刷新，过期结果及旧定时回调被丢弃；执行器拒绝任务不会向界面抛出异常。
- 重扫预约从受理到完成保持单操作占用：排队任务可取消，已开始任务跨同一控制器的停止/启动保留占用。首次提交前再次校验会话，已发出的 IPC 不承诺撤销；提交及回滚使用同一 preferences。`ScanRequests.save()` 区分成功、失败已恢复和状态不确定，精确恢复原键存在状态并确认回滚提交结果。`requestUncertain` 独立于扫描超时，同一绑定下轮询/前后台切换不能清除，须新绑定成功读取后才能解锁。
- 第三阶段最终 JVM 测试共 91 项（90 通过、0 失败、1 项 DexKit native 条件跳过），Lint、Debug/Release/AndroidTest APK 构建通过。2026-09-14 Android 12 / API 32 模拟器上曾运行全部 36 项 instrumentation 测试并通过；最终提交前守卫补充后已重新构建，新增 `ScanLifecycleTest` 使测试源码增至 37 项。收尾时无连接设备或可用 AVD，新增生命周期测试、最终版本设备重跑、可见渲染及真实 LSPosed 服务/延迟 IPC 验证仍留到第九阶段，详见 [优化计划](docs/优化计划.md)。


## 文档同步

- `README.md` 面向模块用户，仅在用户需要了解的功能、安装使用方式或已知限制发生变化时更新；不要每次开发或修复后都往 README 添加说明。
- 开发约束、实现细节、构建与发布流程、测试记录及维护注意事项写入 `AGENTS.md`，不要写入 README。较长的技术说明可放入 `docs/` 并在本文件引用。
- 新增 Hook 时，应在代码或开发文档中说明目标、定位依据、默认状态和安全降级方式；README 只保留用户实际需要的信息。

