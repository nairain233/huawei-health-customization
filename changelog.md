# 更新日志

本文件记录华为运动精简的版本更新内容。

## v1.2 - 2026-09-09

本版本完善跨版本适配能力，并新增后台服务精简功能。

### 新增

- 新增“后台服务精简”，可读取当前用户下华为运动健康声明的服务，按服务类名或进程搜索，并逐项管理屏蔽规则。
- 支持屏蔽目标主进程主动发起的显式服务启动、绑定和前台服务启动；服务屏蔽总开关与界面精简独立，默认关闭且默认放行。
- 引入 DexKit 跨版本定位，并按功能独立记录匹配结果。
- 新增扫描状态、匹配数量、检查数量、目标版本、更新时间和服务状态展示。
- 支持扫描结果缓存，以及在目标应用下次启动时执行预约重扫。
- 新增自动化测试、发布构建、APK 签名与校验流程。

### 优化

- 扫描、缓存加载和报告写入移至后台执行，不阻塞目标应用启动。
- 仅接受唯一且通过签名、内容身份和业务特征校验的定位结果；未命中、歧义或校验失败的功能保持原界面，并独立跳过其他功能继续安装。
- 优化页面 Hook 的事务安装和失败隔离，降低单个功能解析失败对目标应用及其他精简项的影响。
- 调整扫描结果展示位置，使诊断信息更贴近对应的布局精简功能。

### 兼容性与限制

- 目标应用仍为华为运动健康 `com.huawei.health`，静态定位基准为 `17.0.7.310`（versionCode `1700007310`）。
- 其他版本仅按实际 DexKit 扫描结果逐项启用，不代表所有版本完整兼容；未知版本的动态卡片、列表项目和下发区块默认保留。

## v1.1 - 2026-09-06

仅UI更新，设置界面与布局精简入口更新。

### 新增

- 新增独立的“布局精简”入口，集中管理各页面的精简项目。
- 将健康、运动、设备、“我的”和底栏精简项目拆分为独立分类页面。
- 按页面顶部、快捷入口、卡片、推荐内容、设备提示等用途对精简项目进行分组展示。
- 新增“主题设置”，支持跟随系统、浅色模式和深色模式。

### 兼容性与限制

- 目标应用包名、支持版本和 Hook 范围保持不变，仍仅支持华为运动健康 `17.0.7.310`（versionCode `1700007310`）。
- 修改设置后仍需彻底重启华为运动健康两次才能生效。

## v1.0 - 2026-09-05

首个正式版本。

### 新增

- 基于 libxposed API 102 实现华为运动健康界面精简。
- 提供模块设置界面，支持总开关及各精简项独立开关。
- 支持精简健康页的顶部入口、快捷功能、健康卡片与推荐内容。
- 支持精简运动页的顶部入口、课程分类与推荐内容。
- 支持精简设备页的顶部入口、提示区域、设备内容与商城内容。
- 支持精简“我的”页面中的账号卡片、功能入口、分组项目与设置项目。
- 支持隐藏健康、运动、会员、设备和我的底栏标签，并保持原始标签索引。
- 支持隐藏模块应用的桌面图标。
- 提供模块状态、版本信息、诊断信息与使用说明。

### 兼容性

- 目标应用包名：`com.huawei.health`。
- 仅支持华为运动健康 `17.0.7.310`（versionCode `1700007310`）。
- 仅在目标应用主进程中安装 Hook；版本、进程或符号不匹配时自动跳过。

### 稳定性与安全

- 各精简功能相互隔离，单项解析或安装失败不会阻断其他功能。
- 未识别的动态卡片、列表项目和下发区块默认保持显示。
- 配置读取失败时使用安全默认值，不影响目标应用正常启动。
- 日志进行脱敏处理，不记录账号、设备、运动或健康隐私数据。
